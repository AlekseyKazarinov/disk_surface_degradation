disk.py describes a model of a disk with files in it.
In module simulation.py are implemented parameters of simulation (params) and class Simulation. The last one
allows to prepare the disk according set parameters, to gather stats from the disk, and to "add" bad blocks to the disk.